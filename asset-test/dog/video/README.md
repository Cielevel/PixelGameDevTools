# dog/video — 边牧像素视频（AI 生成 → 像素动画标准化流水线溯源）

> 本目录记录 `asset-test` 的视频标准化案例：AI 生成像素视频 → `pixcli video-std` 标准化。
> 时间：2026-09-06。工具：本仓 `pixel-toolkit/`（`pixcli video-std`，依赖 ffmpeg）。
> 标准化产物（帧序列/GIF/HTML/sheet）见 `../product/`，本目录只存源视频与决策记录。

## 文件清单

| 文件 | 说明 |
|------|------|
| `dog_idle.mp4` | **源（绿幕版）**：即梦 AI 生成像素视频，边牧 Idle 动画，绿幕背景（1280×720，24fps，10s，240 帧，H.264+AAC，~9.7MB）。曾命名 `origin_video_greenbg.mp4`，已规范化 |
| `dog_walk.mp4` | **源（绿幕版）**：边牧 Walk 动画（1280×720，24fps，4s，96 帧，H.264，~0.9MB）。用于验证帧间主体位置稳定性（实测：源形心 X 极差 4px 平滑、Y 高频 ±4-7px 为 AI 帧噪声但 24fps 播放平滑） |
| `dog_run_error.mp4` | **源（绿幕版）**：边牧 Run 动画（1280×720，24fps，4s，96 帧，H.264，~0.9MB）。待测（Run 步幅更大，主体位置稳定性与 walk 同口径检查） |
| `README.md` | 本溯源文档 |

> **历史说明**：早期灰底源 `origin_video.mp4`（1280×720，8s，192 帧）已删除——灰底与角色灰衣/灰身撞色，
> 标准化中部分部位被误判透明（实测主体仅 ~30%）。它只进过本地未推送历史，已用 filter-branch 彻底抹除，
> 现在的提交链从一开始就不含它。**绿幕版为唯一源**：绿幕抠像基于色度（G≫R/B），灰衣灰身不受影响。

## 视频特征（实测 2026-09-06）

- 内容：边牧 Idle 动画（轻微摆动/转头），绿幕背景（约 (20,180,40)），角色位于画面中部
- 角色运动范围（全程合并 bbox，alpha 判据）：约 (476,166)-(742,546) = 266×380 px
- **网格检测不可靠**：帧间检测 cell 2×4~6×6 乱跳、conf 0.57~0.70——AI 动态视频是平滑运动+深度压缩，
  **无稳定逻辑网格**，因此不能走 `standardize --grid` 网格还原，走 `video-std` 像素化降采样

## 标准化管线（`pixcli video-std`）

1. **抽帧**：ffmpeg 解码 → 原生帧 PNG（默认全帧率，`--fps` 可重采样）
2. **绿幕抠像**（默认 `--key auto`，四角检测到绿幕自动启用）：色度判定（G > R*1.1 且 G > B*1.1 且差值 >40）→ 绿色像素转 alpha=0，**原始分辨率做，灰衣灰身不受影响**；无绿幕时回落为背景连通清除（`--bg`）
3. **裁剪**：抠像后基于 **alpha**（非颜色差）计算全程合并 bbox；`--crop fixed --box` 手动覆盖
4. **降采样**：`--size 64x64` 面积平均降采样（透明像素不参与色平均；格内不透明占比 ≥1/2 → 不透明，否则透明，alpha 两态）；**`--fit contain`（默认）等比适配居中留边，保持角色比例不变形**
5. **量化**：`--colors 16` 跨帧共享色板 OKLab k-means（防闪烁）；`--outline` 时主体量化 k=colors-1 留描边配额
6. **描边**（可选）：`--outline #000000`（**工程定稿：纯黑 1px 内描边**；深蓝 `#182b54` 为 demo 遗留色，已弃用）
7. **输出**：帧序列 `<名>_NN.png` + `<名>.gif` + `<名>.html`（播放器）；后续可 `pixcli sheet` 拼横向 sprite sheet

## 关键决策记录

1. **降采样而非网格还原**：AI 动态视频无稳定网格（实测证据见上），网格还原不适用；平滑动画→像素图用面积平均。
2. **固定裁剪框**：`auto` 用**全程合并 bbox**（Idle 运动幅度小，固定框帧间稳定），不用逐帧跟踪（避免裁剪抖动）。
3. **色数 ≤16 含描边**：描边时主体量化到 15 色 + 描边 1 色 = 16 色；跨帧合并聚类保证帧间不闪色。
4. **背景透明化在量化前**：背景色不进色板（否则深灰背景占 51% 会吃配额）；连通清除不开洞。
5. **绿幕 vs 背景连通清除**：灰底视频因"背景与角色灰衣/灰身同色系"被误删（实测灰底版主体仅 ~30%）；
   绿幕版色度判定（绿幕绿 G≈180 ≫ R/B），灰衣 (150,150,150) 灰身均不受影响（实测主体占比 63%，完整）。
6. **等比适配（`--fit contain`）**：早期版本把非方形裁剪框（349×571）直接压到方形 64×64，角色被压扁；
   改为等比缩放 + 方形画布居中透明留边（修复后角色 bbox 38×63，比例 0.603 与裁剪框一致），不变形。
7. **描边色定稿纯黑**：`#000000`（195px 描边）；深蓝 `#182b54` 为 demo 遗留色，视觉带蓝紫调，已弃用。
8. **人工门禁**：`pixcli check --size 64x64 --max-colors 16 <帧序列>` 验收（P2 孤立像素提示为边缘碎点，非拦截）；
   `pixcli anim` 看运动数据（面积/帧间 diff 应平稳，无跳变）。

## 复现命令

```bash
# 绿幕版完整标准化（12fps 采样 122 帧，64×64，16 色含描边，透明背景，等比保形）
python3 pixel-toolkit/pixcli.py video-std asset-test/dog/video/dog_idle.mp4 \
    -o <out_dir>/ --size 64x64 --outline '#000000' --colors 16

# 验收
python3 pixel-toolkit/pixcli.py check --size 64x64 --max-colors 16 <out_dir>/<名>_0*.png

# 拼横向 sprite sheet（→ ../product/）
python3 pixel-toolkit/pixcli.py sheet <out_dir>/<名>_0*.png -o <名>_sheet.png
```
